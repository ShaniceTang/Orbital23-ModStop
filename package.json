{
  "dependencies": {
    "cors": "^2.8.5",
    "jsonwebtoken": "^9.0.0",
    "validator": "^13.9.0"
  },
  "scripts": {
    "build": "cd backend && npm install && cd ../frontend && npm install && npm run build",
    "start": "cd frontend && npm start"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
